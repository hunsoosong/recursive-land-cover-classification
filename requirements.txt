# Pip-only fallback. Use Python 3.11.
# Install PyTorch separately following https://pytorch.org/get-started/locally/
numpy>=1.24,<2.1
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyarrow>=14
tqdm
tabulate
# torch>=2.1   # install via official PyTorch instructions for your CUDA
